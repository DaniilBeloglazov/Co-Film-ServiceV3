spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/co_film
    username: whiteyesx
    password: whiteyesx
  flyway:
    user: whiteyesx
    password: whiteyesx
    url: jdbc:postgresql://localhost:5432/co_film
  jpa:
    properties:
      hibernate:
        show_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        highlight_sql: true
    hibernate:
      ddl-auto: none
#  sql:
#    init:
#      mode:
#      platform: all